package client

import (
	"context"

	"github.com/ethereum/go-ethereum/common"
	"github.com/shopspring/decimal"
)

func (c *Clients) TokenPrice(ctx context.Context, tokenAddress common.Address) (*decimal.Decimal, error) {

	return nil, nil
}
